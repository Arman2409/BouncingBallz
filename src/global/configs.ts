const fallSpeed = 15;
const bounceSpeed = 7.5;
const bounceDeviationRange = [1, 2];
const bounceHeightRange = [0.3, 0.6];
const ballRadius = 25;
const rollingSpeed = 1.5;
const stopHeightExtra = 50;
const colorsPerBall = 5;
const colors = ["red", "black", "green", "white",
    "purple", "orangered", "gold", "blue",
    "grey", "orange", "yellow", "olive", "cyan"]
